create table team
(
   team_id integer not null,
   team_name varchar(255) not null,
   primary key(team_id)
);

CREATE TABLE player (
  player_id     INTEGER, 
  player_name   TEXT, 
  player_num INTEGER,
  player_position TEXT
  
);